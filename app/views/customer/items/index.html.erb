<h2>商品一覧</h2>

<% @items.each do |item| %>
	<div class="item-box">
		<p><%= item.genre.name %></p>
		<%= link_to item_path(item) do %>
			<P><%= item.name %></P>
		<% end %>
		<p><%= item.price %>円</p>
	</div>
<% end %>
